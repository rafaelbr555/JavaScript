/*var igerar = window.document.getElementById('igerar')
igerar.addEventListener("click", gerar)

function gerar(){
    var numero = window.document.getElementById('inumero').value
    var resultado = window.document.getElementById('iresultado')

    for(var c = 1; c <= 10; c++){

        resultado.inert = `${c}* ${numero}` 
    }

}
*/

var n = 5

for(var c = 1; c <= n; n++){

    console.log(`${c}*${n}`)
}